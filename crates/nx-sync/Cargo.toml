[package]
name = "nx-sync"
version = "0.1.1"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
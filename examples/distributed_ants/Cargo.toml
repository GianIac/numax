[package]
name = "distributed_ants"
version = "0.1.3"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
nx-sdk = { version = "0.1.3", path = "../../crates/nx-sdk" }

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
panic = "abort"

[workspace]
